import React from 'react';
import ReactDOM from 'react-dom';
import Profile from './App'; // Assuming your Profile component is exported as default from App.js

ReactDOM.render(
  <React.StrictMode>
    <Profile />
  </React.StrictMode>,
  document.getElementById('root')
);
